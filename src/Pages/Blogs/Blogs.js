import React from 'react';
import useTitle from '../../Hooks/useTitle';

const Blogs = () => {
    useTitle('Blogs')
    return (
        <div>
            this is blogs
        </div>
    );
};

export default Blogs;